{
  "run": [
    {
      "method": "shell.run",
      "params": {
        "message": "git pull"
      }
    },
    {
      "method": "shell.run",
      "params": {
        "message": "{{os.platform() === 'win32' ? 'setup.bat' : 'chmod +x setup.sh && ./setup.sh'}}",
        "on": [{ "event": "/Setup Complete/", "done": true }]
      }
    }
  ]
}
