{
  "run": [
    {
      "method": "shell.run",
      "params": {
        "message": "git pull"
      }
    },
    {
      "method": "shell.run",
      "params": {
        "message": "{{os.platform() === 'win32' ? 'call setup.bat --update && exit' : 'chmod +x setup.sh && ./setup.sh --update && exit'}}",
        "on": [{ "event": "/Setup Complete/", "done": true }]
      }
    }
  ]
}
